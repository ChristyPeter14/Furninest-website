<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sales Report</title>
    <style>
        /* Reset some default styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        /* Basic body styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f9fc;
            color: #333;
            padding: 20px;
        }

        /* Title styling */
        h1 {
            text-align: center;
            color: #2c3e50;
            font-size: 2em;
            margin-bottom: 20px;
        }

        /* Paragraphs for summary details */
        p {
            font-size: 1em;
            color: #555;
            margin: 5px 0;
        }

        /* Styling the table */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
            background-color: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Table header styling */
        th {
            background-color: #3498db;
            color: #fff;
            padding: 10px;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 0.9em;
            border: 1px solid #ddd;
        }

        /* Table row styling */
        td {
            padding: 12px;
            text-align: center;
            border: 1px solid #ddd;
            color: #333;
            font-size: 0.9em;
        }

        /* Alternating row colors for readability */
        tbody tr:nth-child(odd) {
            background-color: #f4f4f4;
        }

        tbody tr:nth-child(even) {
            background-color: #e9e9e9;
        }

        /* Responsive styling for mobile */
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }
            h1 {
                font-size: 1.5em;
            }
            table, th, td {
                font-size: 0.85em;
            }
        }
    </style>
</head>
<body>
    <h1>Sales Report</h1>
    <p><strong>Total Orders:</strong> <%= totalOrders %></p>
    <p><strong>Total Customers:</strong> <%= totalCustomers %></p>
    <p><strong>Total Transaction Amount:</strong> ₹<%= totalTransaction.toFixed(2) %></p>
    <p><strong>Online Payments:</strong> <%= onlinePayments %></p>
    <p><strong>Cash on Delivery:</strong> <%= cashOnDelivery %></p>
    <p><strong>Cancelled Orders:</strong> <%= orderCancelled %></p>
    <p><strong>Total Discounts:</strong> ₹<%= totalDiscounts.toFixed(2) %></p>
    <p><strong>Total Coupons:</strong> ₹<%= totalCoupons.toFixed(2) %></p>

    <table>
        <thead>
            <tr>
                <th>Order ID</th>
                <th>User</th>
                <th>Original Total</th>
                <th>Total Amount</th>
                <th>Discount Amount</th>
                <th>Discount Percentage</th>
                <th>Coupon</th>
                <th>Status</th>
                <th>Order Date</th>
            </tr>
        </thead>
        <tbody>
            <% orders.forEach(order => { %>
                <tr>
                    <td><%= order.orderId %></td>
                    <td><%= order.userId %></td>
                    <td>₹<%= order.originalTotal %></td>
                    <td>₹<%= order.totalAmount %></td>
                    <td>₹<%= order.discountAmount %></td>
                    <td><%= order.totalDiscountPercentage %> %</td>
                    <td><%= order.coupon %></td>
                    <td><%= order.orderstatus %></td>
                    <td><%= order.orderDate %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>
